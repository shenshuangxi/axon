package com.sundy.axon.common;

/**
 * 接口用于描述一个组件可以被其他组件订阅，如{@link CommandBus} {@link EventBus}
 * @author Administrator
 *
 *该组件在新版本已删除 因为如果一个组件可以被订阅，而有包含一个可订阅组件时会出现问题
 */
public interface Subscribable {

	void unsubscribe();
	
	void subscribe();
	
}

package com.sundy.axon.domain;

/**
 * DomainEventStream 代表一条历史领域事件流。该事件排序代表事件发生的先后顺序
 * @author Administrator
 *
 */
public interface DomainEventStream {

	boolean hasNext();
	
	DomainEventMessage next();
	
	DomainEventMessage peek();
}
